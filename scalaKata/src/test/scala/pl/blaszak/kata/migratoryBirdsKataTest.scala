package pl.blaszak.kata

import org.scalatest.FunSuite

class migratoryBirdsKataTest extends FunSuite {

  test("should ") {
    assert(migratoryBirdsKata.migratoryBirds(Array(1, 4, 4, 4, 5, 5, 5, 3)) === 4)
  }

}
